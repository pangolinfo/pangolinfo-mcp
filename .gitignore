node_modules/
dist/
*.log
.env
.env.*
!.env.example
.DS_Store

# 部署脚本: 含真实 ACR 密码的 .sh 不进仓库; 模板 .example 进.
# 跟 ext-scrapeapi 同样的结构 (scripts/window/).
scripts/window/docker-mcp.sh
!scripts/window/docker-mcp.sh.example

# 兼容老路径 (万一有人在 deploy/ 下手动建了)
deploy/deploy-mcp.sh
deploy/deploy-mcp.cmd
deploy/.local
deploy/secrets/
deploy/*.local.yaml

# 本地用的 ACK YAML 粘贴模板 (含 redeploy-timestamp 等频繁变的字段)
deploy/k8s-deployment-aliyun-paste.yaml

# Windows 误产物 (printf >/dev/null 在 cmd 下会被解释为创建 nul 文件)
nul

# kubeconfig / 阿里云 cli 的临时凭据
.kube/
*.kubeconfig
.aliyun/

# MCP Registry publisher CLI (downloaded binary, not source)
mcp-publisher
mcp-publisher.exe
mcp-publisher.tar.gz
mcp-publisher*.tar.gz
# DNS auth private key — NEVER commit
mcp_dns_key.pem
*.ed25519.key
